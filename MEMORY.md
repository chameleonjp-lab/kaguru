# Implementation Memory

生成画像はプロジェクト外のWebDevストレージURLを参照する。大きな画像をプロジェクトツリーへ追加しない。画面の主役はウォールナットまたはオークの板材であり、ロゴは年輪アンバーの検品印として使用する。

BabylonエンジンはReact 19のStrictMode対策として `GameCanvas` のrefで一度だけ生成し、シーン・エンジン・DOMリスナーをアンマウント時に破棄する。`?demo` は検証用に加工済みの板から継ぎ目処理を自動実行する。

